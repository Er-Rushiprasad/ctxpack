# CLAUDE.md

Companion file for automated agents and contributors.

Read `ARCHI.md` then `PLAN.md` at the start of any coding session. Update
those files (and this one) when architecture or plan decisions change.

Short checklist:
- Read `ARCHI.md` and `PLAN.md` before coding.
- Follow project layout: `extension/` and `server/` top-level folders.
- Use `server/README.md` to run the local companion server during development.